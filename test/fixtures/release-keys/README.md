# Fixture signing keys

These Ed25519 keys exist only for automated metadata tests. They are public test material, are not authorized by any production trust root, and must never sign a published Release Channel or Release Manifest.
